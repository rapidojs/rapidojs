import { container } from 'tsyringe';

/**
 * The global dependency injection container for Rapido.js.
 * All services, controllers, and other injectables are registered here.
 */
export const rapidoContainer = container;
